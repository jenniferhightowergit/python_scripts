if len(label_cols) == 1:
    report = classification_report(
        y,
        y_pred.ravel(),
        labels=[0, 1],
        target_names=[f"NOT_{label_cols[0]}", label_cols[0]],
        output_dict=True,
        zero_division=0
    )
else:
    report = classification_report(
        y,
        y_pred,
        target_names=label_cols,
        output_dict=True,
        zero_division=0
    )
