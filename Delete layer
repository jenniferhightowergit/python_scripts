join lateral (
    select
        e.meter_removal_date
    from exp_dates e
    where e.meter_removal_date between
          coalesce(b."INSTALLATION_DATE", date '2001-01-01')
          and coalesce(b."meter_removal_date", date '3001-01-01')
    order by random()
    limit 1
) c on true
