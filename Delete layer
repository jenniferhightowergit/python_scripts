sql = """ ...your whole script... """

stmts = [s.strip() for s in sql.split(";") if s.strip()]

for i, s in enumerate(stmts, 1):
    head = " ".join(s.split())[:70]
    try:
        conn.execute(s)
        print(f"{i:>3} ok   {head}")
    except Exception as e:
        print(f"{i:>3} FAIL {head}")
        print(f"     {e}")
        break
