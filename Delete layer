category_cols = list(cat_schema.keys())

for col in category_cols:
    df[col] = (
        df[col].astype("string")
        .str.replace("\x00", "", regex=False)
        .str.strip()
        .fillna("UNKNOWN")
        .astype(CategoricalDtype(categories=cat_schema[col]))
    )
